// Timing constants — adjust to tune perceived responsiveness

/** How long each reasoning step label is shown (ms) */
export const REASONING_STEP_MS = 900;

/** Delay after reasoning steps before streaming begins (ms) */
export const ANSWER_START_DELAY_MS = 300;

/** Delay between each streamed character chunk (ms) */
export const STREAM_CHUNK_MS = 12;

/** Minimum total time spent in reasoning phase (ms) */
export const MIN_REASONING_MS = 1800;
